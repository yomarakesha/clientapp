# Клиент видеонаблюдения - MediaMTX

Десктопное приложение для видеонаблюдения на Python, аналогичное iVMS-4200, для подключения к серверу MediaMTX через RTSP.

## 🎯 Особенности

- ✅ Поддержка до **100 камер** одновременно на одном экране
- ✅ **8 раскладок**: 1x1, 2x2, 3x3, 4x4, 5x5, 6x6, 8x8, 10x10
- ✅ Автоматическое переподключение при обрыве связи
- ✅ Полноэкранный режим для любой камеры
- ✅ Создание скриншотов
- ✅ Темная тема интерфейса
- ✅ Русская локализация
- ✅ Индикаторы статуса подключения
- ✅ Overlay с названием камеры, временем и статусом

## 📋 Требования

- Windows 10/11
- Python 3.11+
- MediaMTX сервер (или любой RTSP сервер)

## 🚀 Установка

### 1. Клонирование/Распаковка проекта

```bash
cd clientapp
```

### 2. Установка зависимостей

```bash
pip install -r requirements.txt
```

## 🎬 Запуск

```bash
python main.py
```

## 📖 Использование

### Добавление камеры

1. Нажмите кнопку **"➕ Добавить камеру"** на панели инструментов (или `Ctrl+N`)
2. Заполните форму:
   - **Название**: имя камеры для отображения
   - **RTSP URL**: адрес потока (например: `rtsp://192.168.1.100:8554/camera1`)
   - **Логин/Пароль**: если требуется аутентификация
   - **Группа**: для организации камер (опционально)
   - **Позиция**: позиция в сетке или автоматически
3. Нажмите **"Сохранить"**

### Формат RTSP URL для MediaMTX

```
rtsp://[server_ip]:8554/[stream_name]
```

Примеры:
- `rtsp://192.168.1.100:8554/camera1`
- `rtsp://10.0.0.50:8554/entrance`

Если требуется аутентификация:
- Логин и пароль вводятся в отдельные поля диалога
- Приложение автоматически формирует URL: `rtsp://username:password@server_ip:8554/stream_name`

### Управление раскладкой

1. Нажмите **"📐 Раскладка"** на панели инструментов
2. Выберите нужный размер сетки:
   - 1x1 (1 камера)
   - 2x2 (4 камеры)
   - 3x3 (9 камер)
   - 4x4 (16 камер)
   - 5x5 (25 камер)
   - 6x6 (36 камер)
   - 8x8 (64 камеры)
   - 10x10 (100 камер)

### Полноэкранный режим

- **Двойной клик** на любую камеру для входа в полноэкранный режим
- Нажмите **ESC** или **двойной клик** снова для выхода

### Скриншоты

- Нажмите **"📷 Скриншот"** (или `Ctrl+S`)
- В обычном режиме сохраняются скриншоты всех активных камер
- В полноэкранном режиме сохраняется скриншот отображаемой камеры
- Скриншоты сохраняются в папку `screenshots/`

### Управление потоками

- **▶️ Запустить все** - запуск потоков для всех камер
- **⏹️ Остановить все** - остановка всех потоков

## ⚙️ Конфигурация

Настройки приложения хранятся в файле `config.json`:

```json
{
  "cameras": [],           // Список камер
  "layout": 4,            // Текущая раскладка (количество ячеек)
  "theme": "dark",        // Тема интерфейса
  "language": "ru",       // Язык интерфейса
  "fps_limit": 15,        // Ограничение FPS для оптимизации
  "auto_reconnect": true, // Автоматическое переподключение
  "reconnect_interval": 5 // Интервал переподключения (сек)
}
```

Вы можете изменять эти параметры вручную или через интерфейс приложения.

## 🎨 Горячие клавиши

| Клавиша | Действие |
|---------|----------|
| `Ctrl+N` | Добавить новую камеру |
| `Ctrl+S` | Создать скриншот |
| `ESC` | Выход из полноэкранного режима |
| Двойной клик | Полноэкранный режим |

## 📁 Структура проекта

```
clientapp/
├── main.py                 # Точка входа
├── requirements.txt        # Зависимости
├── config.json            # Конфигурация
├── models/
│   ├── camera.py          # Модель Camera
│   └── stream.py          # Модель Stream
├── core/
│   ├── stream_manager.py  # VideoStreamThread
│   └── camera_manager.py  # CameraManager
├── ui/
│   ├── main_window.py     # Главное окно
│   ├── video_widget.py    # Виджет видео
│   ├── grid_widget.py     # Сетка камер
│   └── camera_dialog.py   # Диалог камеры
├── utils/
│   ├── config.py          # ConfigManager
│   └── logger.py          # Логирование
├── logs/                  # Логи приложения
└── screenshots/           # Скриншоты
```

## 🔧 Оптимизация производительности

Для работы с большим количеством камер (50+):

1. **FPS Limit**: По умолчанию установлен на 15 FPS. Для большего количества камер можно снизить до 10-12 FPS в `config.json`
2. **Раскладка**: Используйте раскладку, соответствующую количеству активных камер
3. **Ресурсы**: Для 100 камер рекомендуется минимум 16 GB RAM

## 📝 Логи

Логи приложения сохраняются в директории `logs/`:
- Формат: `video_client_YYYYMMDD.log`
- Уровень логирования: DEBUG в файл, INFO в консоль

## ❗ Решение проблем

### Камера не подключается

1. Проверьте правильность RTSP URL
2. Убедитесь, что MediaMTX сервер запущен и доступен
3. Проверьте логин/пароль если требуется аутентификация
4. Проверьте логи приложения в папке `logs/`

### Низкая производительность

1. Уменьшите `fps_limit` в `config.json`
2. Используйте раскладку с меньшим количеством ячеек
3. Отключите неиспользуемые камеры

### Приложение зависает

- Проверьте наличие проблемных потоков в логах
- Попробуйте остановить все потоки и запустить заново

## 📄 Лицензия

Данный проект создан для демонстрационных целей.

## 🤝 Поддержка

При возникновении проблем:
1. Проверьте логи в папке `logs/`
2. Убедитесь, что все зависимости установлены корректно
3. Проверьте совместимость версий Python и библиотек

---

**Версия**: 1.0  
**Дата**: 2025-12-02
